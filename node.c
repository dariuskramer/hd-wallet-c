#include <stdlib.h>
#include <stdint.h>
#include <secp256k1.h>

int node_master_generate(const uint8_t *seed, size_t seedlen, secp256k1_context *ctx)
{
	(void)seed;
	(void)seedlen;
	(void)ctx;
	return 0;
}
